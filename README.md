# CORA-Context-Option-Ranking-Architecture
CORA - a calibrated multimodal bi-encoder that ranks predefined options against text + image context. Non-autoregressive, closed-set softmax, temperature-scaled probabilities.
